apiVersion: Helm.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: ownapps
  namespace: ownapps
spec:
  releaseName: ownapps
  chart:
    repository: oci://tccr.io/truecharts
    name: ownapps-document-server
  values:
    redis:  
      enabled: false 
